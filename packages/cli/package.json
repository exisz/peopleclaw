{
  "name": "@peopleclaw/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "peopleclaw": "./dist/index.js",
    "pc": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean && chmod +x dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.0.0",
    "typescript": "^5.6.3"
  }
}
